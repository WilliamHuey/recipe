
/**
 * Module dependencies.
 */

var fs = require('fs');

/**
 * Expose `<%= strcase.camelCase(projectName) %>`.
 */

module.exports = <%= strcase.camelCase(projectName) %>;

/**
 * A one-liner.
 *
 * @param {String} name
 * @return {Function}
 * @api public
 */

function <%= strcase.camelCase(projectName) %>(name) {

}
